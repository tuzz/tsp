#include <limits.h>

const int MAX_CITIES = 300;
const int MAX_DAYS = MAX_CITIES + 1;
const int MAX_PRICE = 65535;
const int MAX_WEIGHT = MAX_PRICE * MAX_DAYS;
const int ALPHA = 26;
const int ALPHA2 = ALPHA * ALPHA;
const int ALPHA3 = ALPHA2 * ALPHA;
const int A = 65;
const int ZERO = 48;
const int NINE = ZERO + 9;
const bool T = 1;
const bool F = 0;
